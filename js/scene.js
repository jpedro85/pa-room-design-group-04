import * as THREE from 'three';

/**
 * Initializes and returns a new THREE.Scene object.
 *
 * @returns {THREE.Scene} The initialized scene.
 */
export function initScene() {
    return new THREE.Scene();
}
